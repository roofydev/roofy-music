{
  "name": "roofy-music",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first personal music app built on Feishin, Navidrome, yt-dlp, and ffmpeg.",
  "type": "commonjs",
  "scripts": {
    "postinstall": "node scripts/ensure-desktop-deps.mjs --force",
    "predesktop:dev": "node scripts/ensure-desktop-deps.mjs",
    "predesktop:build": "node scripts/ensure-desktop-deps.mjs",
    "predesktop:typecheck": "node scripts/ensure-desktop-deps.mjs",
    "predesktop:package:dir": "node scripts/ensure-desktop-deps.mjs",
    "predesktop:package:win": "node scripts/ensure-desktop-deps.mjs",
    "desktop:install": "node scripts/ensure-desktop-deps.mjs --force",
    "desktop:dev": "corepack pnpm --dir desktop dev",
    "desktop:build": "corepack pnpm --dir desktop run build",
    "desktop:package:dir": "set PATH=%CD%\\.tools;%PATH%&& corepack pnpm --dir desktop exec electron-builder --dir",
    "desktop:typecheck": "corepack pnpm --dir desktop run typecheck:node && corepack pnpm --dir desktop run typecheck:web",
    "desktop:package:win": "corepack pnpm --dir desktop run download:sidecars && corepack pnpm --dir desktop run build && corepack pnpm --dir desktop exec electron-builder --win --x64 --publish never",
    "legacy:mvp": "node src/server/index.js",
    "legacy:check": "node --check src/server/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
